import Link from "next/link"; import {CheckCircle2,ShieldAlert} from "lucide-react"; import {Logo} from "@/components/brand/Logo"; import {successMessage,whatsappUrl} from "@/lib/whatsapp";
export const metadata={title:"Solicitação recebida",robots:{index:false,follow:false}};
export default async function Success({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;const protocol=(p.protocol||"Protocolo indisponível").slice(0,30);return <main className="grid min-h-screen place-items-center bg-cream px-5 py-12"><div className="w-full max-w-2xl rounded-3xl border bg-white p-7 text-center shadow-premium sm:p-12"><Logo/><CheckCircle2 className="mx-auto mt-10 text-[#178A5B]" size={64}/><h1 className="mt-6 font-serif text-4xl font-semibold">Solicitação recebida.</h1><p className="mt-4 leading-7 text-muted">Nossa especialista analisará as informações enviadas e entrará em contato pelo WhatsApp.</p><div className="mx-auto mt-7 max-w-sm rounded-2xl bg-cream p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted">Seu protocolo</p><p className="mt-2 text-xl font-bold">{protocol}</p></div><p className="mt-7 flex items-center justify-center gap-2 text-sm text-muted"><ShieldAlert size={18}/>Não envie senhas, códigos de autenticação ou dados bancários por mensagens.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><a className="btn-primary" target="_blank" rel="noreferrer" href={whatsappUrl(successMessage(protocol,"Cliente","Solicitação enviada pelo site"))}>Falar com a especialista</a><Link className="btn-secondary" href="/">Voltar para o início</Link></div></div></main>}
