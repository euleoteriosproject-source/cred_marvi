import {Mail,MessageCircle,Instagram,Linkedin,Clock} from "lucide-react"; import {Header} from "@/components/layout/Header"; import {Footer} from "@/components/layout/Footer"; import {Container} from "@/components/common/Container"; import {siteConfig} from "@/lib/site-config"; import {whatsappUrl} from "@/lib/whatsapp";
export const metadata={title:"Contato"};
const items=[[MessageCircle,"WhatsApp",siteConfig.whatsappNumber?"Iniciar conversa":"Canal a configurar",whatsappUrl("Olá! Gostaria de falar com a Marvi Finance.")],[Mail,"E-mail",siteConfig.email||"E-mail a configurar",siteConfig.email?`mailto:${siteConfig.email}`:"#"],[Instagram,"Instagram","Perfil oficial a configurar",siteConfig.instagramUrl||"#"],[Linkedin,"LinkedIn","Perfil oficial a configurar",siteConfig.linkedinUrl||"#"],[Clock,"Atendimento",siteConfig.serviceHours,"#"]] as const;
export default function Contact(){return <><Header/><main className="bg-cream py-16 sm:py-24"><Container><p className="eyebrow">Fale conosco</p><h1 className="section-title">Estamos prontos para ouvir você.</h1><p className="mt-5 max-w-2xl leading-7 text-muted">Escolha um dos canais oficiais. Nunca envie senhas, códigos de autenticação ou dados bancários completos.</p><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(([Icon,title,text,href])=><a key={title} href={href} target={href.startsWith("http")?"_blank":undefined} rel="noreferrer" className="rounded-2xl border bg-white p-6 transition hover:border-gold hover:shadow-lg"><Icon className="text-[#9a7611]"/><h2 className="mt-5 font-bold">{title}</h2><p className="mt-2 text-sm text-muted">{text}</p></a>)}</div></Container></main><Footer/></>}
