"use client"; import {useState} from "react"; import Link from "next/link"; import {Menu,X} from "lucide-react"; import {Logo} from "@/components/brand/Logo"; import {Container} from "@/components/common/Container";
const links=[["Soluções","/#solucoes"],["Como funciona","/#como-funciona"],["Especialista","/#especialista"],["Segurança","/#seguranca"],["Dúvidas","/#duvidas"]];
export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-slate-200/70 bg-white/90 backdrop-blur"><Container className="flex h-20 items-center justify-between"><Logo/><nav className="hidden items-center gap-7 lg:flex" aria-label="Principal">{links.map(([l,h])=><Link className="nav-link" key={h} href={h}>{l}</Link>)}</nav><div className="flex items-center gap-2"><Link href="/analise" className="btn-primary hidden sm:inline-flex">Iniciar análise</Link><button className="grid h-12 w-12 place-items-center rounded-xl lg:hidden" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Abrir menu">{open?<X/>:<Menu/>}</button></div></Container>{open&&<nav className="border-t bg-white p-5 lg:hidden">{links.map(([l,h])=><Link onClick={()=>setOpen(false)} className="block rounded-lg px-3 py-3 font-medium" key={h} href={h}>{l}</Link>)}<Link href="/analise" className="btn-primary mt-3 w-full">Iniciar análise</Link></nav>}</header>}
