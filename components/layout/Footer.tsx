import Link from "next/link";
import {Container} from "../common/Container";
import {Logo} from "../brand/Logo";
import {siteConfig} from "@/lib/site-config";
import {siteContactMessage,whatsappUrl} from "@/lib/whatsapp";

export function Footer(){return <footer className="bg-navy py-14 text-slate-300"><Container><div className="grid gap-10 md:grid-cols-3"><div><Logo light/><p className="mt-5 max-w-sm text-sm leading-6">Atendimento financeiro consultivo para pessoas e empresas.</p></div><div><h2 className="font-semibold text-white">Informações</h2><div className="mt-4 flex flex-col gap-3 text-sm"><Link href="/solucoes">Soluções</Link><Link href="/privacidade">Privacidade</Link><Link href="/termos">Termos de Uso</Link><Link href="/contato">Contato</Link></div></div><div><h2 className="font-semibold text-white">Canais oficiais</h2><a href={whatsappUrl(siteContactMessage())} target="_blank" rel="noreferrer" className="mt-4 inline-flex text-sm font-semibold text-white transition hover:text-gold">WhatsApp oficial</a>{siteConfig.email&&<a className="mt-2 block text-sm transition hover:text-white" href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>}{siteConfig.cnpj&&<p className="mt-2 text-sm">CNPJ {siteConfig.cnpj}</p>}</div></div><div className="mt-12 border-t border-white/10 pt-7 text-xs leading-5 text-slate-400">A disponibilidade, aprovação, taxas e condições dependem das instituições responsáveis e da análise de cada solicitação. © {new Date().getFullYear()} Grupo Marvi.</div></Container></footer>}
